<!DOCTYPE html>
<html lang="hu">
<head>
    <%- include('sections/head.ejs') %>
    <link rel="stylesheet" href="/trds/tr-theme.css">
    <script defer src="/trds/trds.js"></script>
</head>
<body>

    <%- include('sections/header.ejs') %>

    <main>
        <%- include(`pages/${page.name}/${page.name}.ejs`) %>
    </main>

    <%- include('sections/footer.ejs') %>

    <% if(page.name === 'hdr' || page.name === 'pdr' || page.name === 'index'){ %>
        <script type="module" src="/workmedias/dist/WorkmediasShortBundle.js"></script>
    <% } %>

    <% if(page.name === 'works'){ %>
        <script type="module" src="/workmedias/dist/WorkmediasLongBundle.js"></script>
    <% } %>
    
</body>
</html>