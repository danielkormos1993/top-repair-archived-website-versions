{
  "name": "toprepair.hu_v4.0",
  "version": "4.0.0",
  "description": "TOP REPAIR dent removal website",
  "scripts": {
    "build": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npm-watch",
    "start": "netlify dev",
    "deploy": "git push origin master"
  },
  "watch": {
    "build": "src/**/*.*"
  },
  "author": "Daniel Kormos",
  "license": "ISC",
  "devDependencies": {
    "ejs": "^3.1.6",
    "npm-watch": "^0.9.0"
  }
}
